{
  "name": "water-switch-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/server.js --bundle --minify --platform=node --target=node22 --format=esm --outfile=dist/server.js --banner:js=\"import{createRequire}from'module';const require=createRequire(import.meta.url);\"",
    "start": "node dist/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.27.3"
  }
}
